version: '3'
services:
  api:
    image: sample-express-app
    container_name: api
    build: ./api
    networks:
      - backend
    ports:
      - "3000:3000"

networks:
  backend:
    driver: bridge